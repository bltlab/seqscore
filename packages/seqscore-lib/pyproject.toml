[project]
name = "seqscore-lib"
version = "0.7.0"
description = "The library for the seqscore cli application"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
